<build>
    <plugins>
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-compiler-plugin</artifactId>
            <version>3.11.0</version> <!-- Update this version to the latest -->
            <configuration>
                <source>1.8</source> <!-- Adjust the Java version according to your project -->
                <target>1.8</target> <!-- Adjust the Java version according to your project -->
            </configuration>
        </plugin>
    </plugins>
</build>
